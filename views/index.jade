extends layout

block navi
  include navi-index
block content
  div
    div
      ul(class="thumbnails")
        - each project in projects
          li(class="span4")
            div(class="thumbnail")
              h3= project.name
              a(href="/projects/" + project.id, class="btn btn-primary")= project.id
