extends layout

block content
  a(class="btn btn-large btn-primary",href="/login") Login
  div
    div
      ul(class="thumbnails")
        - each project in projects
          li(class="span4")
            div(class="thumbnail")
              h3= project.name
              a(href="/projects/" + project.id, class="btn btn-primary")= project.id
